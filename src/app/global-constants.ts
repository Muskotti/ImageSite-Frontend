import PostsInterface from './components/frontpage/posts/PostsInterface';

export class GlobalConstants {
  // backend url
  public static apiURL = 'http://' + window.location.host + '/'; // backendin osote menee tähän

}
